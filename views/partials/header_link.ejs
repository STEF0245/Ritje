<% if (locals?.selected) { %>
    <a href="<%= url %>" class="theme-link theme-accent-text font-bold">
        <%= text %>
    </a>
<% } else { %>
    <a href="<%= url %>" class="theme-link">
        <%= text %>
    </a>
<% } %>