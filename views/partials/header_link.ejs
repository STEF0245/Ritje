<% if (locals?.main) { %>
    <a href="<%= url %>" class="theme-btn-primary px-6 py-2.5 text-sm">
        <%= text %>
    </a>
<% } else { %>
    <a href="<%= url %>" class="theme-link">
        <%= text %>
    </a>
<% } %>