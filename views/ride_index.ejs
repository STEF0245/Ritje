<!DOCTYPE html>
<html lang="nl" class="text-slate-200">
    <%- include('partials/head', { title }) %>
    
    <body class="theme-body min-h-screen flex flex-col">
		<%- include('partials/header', { user }) %>
        <main class="theme-main flex-1">
            <section class="max-w-5xl mx-auto">
                <div class="text-center mb-8">
                    <p class="theme-kicker">Ritten Overzicht</p>
                    <h1 class="theme-heading">Beschikbare ritten verschijnen hier.</h1>
                    <p class="theme-subtext">Binnenkort zie je hier een lijst met ritten die je kan boeken of beheren.</p>
                </div>
                <div class="theme-panel p-8 text-center text-slate-300">
                    Nog geen ritten om weer te geven.
                </div>
            </section>
        </main>


        <%- include('partials/footer') %>
    </body>
</html>